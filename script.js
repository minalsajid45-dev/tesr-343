document.addEventListener('DOMContentLoaded', () => {initNavigation();initScrollEffects();initCopyIP();initRankFiltering();initStoreCart();initFormValidation();initSmoothScroll();});function initNavigation() {const header = document.querySelector('.navbar');window.addEventListener('scroll', () => {if (window.scrollY > 50) {header.style.background = "rgba(10, 10, 12, 1)";} else {header.style.background = "rgba(10, 10, 12, 0.95)";}});}function initCopyIP() {const copyButtons = document.querySelectorAll('.copy-ip-btn');copyButtons.forEach(btn => {btn.addEventListener('click', () => {const ip = btn.getAttribute('data-ip');const feedback = btn.querySelector('.copy-feedback');navigator.clipboard.writeText(ip).then(() => {if (feedback) {feedback.textContent = 'IP Copied!';feedback.classList.add('visible');setTimeout(() => feedback.classList.remove('visible'), 2000);}});});}}/* Rest of the JavaScript code */